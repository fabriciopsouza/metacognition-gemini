# Regra 03 — File-First

**Fonte única:** `_shared/traceability/SKILL.md` (Regra 1).

Não redefinida aqui. Resumo: ler o arquivo antes de editar/referenciar;
inspecionar a fonte real antes de assumir estrutura de dados
(`df.columns.tolist()`, `DESCRIBE TABLE`, schema inspect). O "porquê"
(causa raiz #2 de retrabalho) está em `_shared/traceability`.
