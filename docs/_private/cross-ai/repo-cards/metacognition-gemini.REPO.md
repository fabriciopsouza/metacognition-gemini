# metacognition-gemini — mãe Gemini (identificador padrão · MODELO)

> Gerado pelo `claude-master` como template simétrico. **O Gemini** commita no próprio repo (é a mãe dele).

| Campo | Valor |
|---|---|
| **Papel** | `master` — repo-mãe canônico |
| **Dono** | `gemini` (id cross-IA: `gemini-master`) |
| **Visibilidade** | privado |
| **Quem escreve** | **só o Gemini** (leitura livre) |
| **Canonical remote** | `https://github.com/fabriciopsouza/metacognition-gemini` (branch `main`, proteger: PR + CI) |

**O que NÃO fazer:** o Gemini **nunca** escreve em `metacognition-framework` (read-only). Cross-IA acontece
**só pelo hub** (`metacognition-hub`), via PR no branch próprio. Simétrico ao lado Claude.
